---
status: living
title: Product Backlog
source: governance-bloat-review-2026-04-12 + optimization-round-2026-05-04 + optimization-research-2026-06-02
created: 2026-04-12
last_updated: 2026-07-16
---

# Product Backlog

> **Completed entries** (Shipped / Cancelled) live in
> [`_product-backlog-archive.md`](./_product-backlog-archive.md). This index
> tracks only active (Pending / In Progress) work. Entry numbers are global and
> stable across both files, so `#N` dependency references resolve in either.

## Source Summary

Governance file bloat review (2026-04-12) identified 10 findings across P0–P2: multiple data surfaces grow unbounded (archive/, Global Lessons, Spec Index, INDEX.jsonl), compaction mechanisms are advisory-only, and process artifacts (_raw-intake archives, superseded L2 entries, shipped specs) accumulate without consumers. Industry patterns (LSM-tree compaction, progressive summarization, tiered retention) converge on a 4-state document lifecycle with LLM-driven summarization at tier transitions.

## Feature Inventory

> The **GH Issue** column links the GitHub tracker (`KbWen/agentic-os`). The
> **Dependencies** column uses bare `#N` = backlog entry numbers (this file).
> Entries #45–#68 came from a 2026-06-02 optimization-research pass mining a more
> mature internal reference implementation. The set was deliberately curated: each
> survivor was cross-checked against this repo's actual code paths over multiple
> verification rounds; items that were premature, already-handled, or
> deliberately-removed-before were dropped (DELETE-bias). The rows below are the
> verified ~1–2 month roadmap. See
> [`../OPTIMIZATION_ROADMAP.md`](../OPTIMIZATION_ROADMAP.md).

| # | Feature | Kind | Labels | Priority | Spec File | Tier | Status | GH Issue | Dependencies |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Tiered Document Lifecycle Engine (4-tier state machine + config) | framework | lifecycle | P1 | docs/specs/tiered-doc-lifecycle.md | feature | Pending | [#140](https://github.com/KbWen/agentic-os/issues/140) | — |
| 3 | Archive directory GC + INDEX.jsonl rotation 2026-07-10 evidence: INDEX.jsonl at 99 lines / archive/ 106 files 1.3MB at month ~3; compaction prohibited by the append-only witness — rotation design must be witness-compatible. | framework | lifecycle | P2 | — | feature | Pending | [#141](https://github.com/KbWen/agentic-os/issues/141) | #1 |
| 7 | Domain Doc L2 superseded entry archival | framework | lifecycle | P2 | — | quick-win | Pending | [#142](https://github.com/KbWen/agentic-os/issues/142) | — |
| 11 | Shipped specs accumulation — status-driven filtering | framework | lifecycle | P2 | — | quick-win | Pending | [#143](https://github.com/KbWen/agentic-os/issues/143) | #1 |
| 13 | Warm→Cold LLM summarization pass in /ship | framework | lifecycle | P2 | — | feature | Pending | [#144](https://github.com/KbWen/agentic-os/issues/144) | #1, #3 |
| 18 | Lightweight routing heuristics (decision tree in config.yaml, not a DSL) | framework | routing | P2 | — | quick-win | Pending | [#148](https://github.com/KbWen/agentic-os/issues/148) | — |
| 21 | Skill cache timestamp + staleness invalidation | framework | skills | P2 | — | quick-win | Pending | [#149](https://github.com/KbWen/agentic-os/issues/149) | — |
| 33 | Claude Code plugin packaging (.claude-plugin/plugin.json + bin/ + commands/agents/hooks bundling, no monitors) | dx | packaging | P2 | — | feature | Pending | [#150](https://github.com/KbWen/agentic-os/issues/150) | #30, #31 |
| 70 | Export structured JSON Drift Log on ship for CI/CD audit trails | framework | governance | P2 | — | feature | Pending | [#193](https://github.com/KbWen/agentic-os/issues/193) | — |
| 69 | RPI→QRSPI flow-adaptation study — Strand D executed as directive-enforcement-audit (spec + ADR-011 + 112-row census + count ratchet); strands A/B/C/E remain open as future candidates | framework | governance | P1 | docs/specs/directive-enforcement-audit.md | feature | Shipped | [#176](https://github.com/KbWen/agentic-os/issues/176) | #45, #65 |
| 72 | zh-TW README FAQ mirror (parity with EN discoverability FAQ added in PR #230) | docs | i18n | P3 | — | quick-win | Shipped | — | — |
| 76 | Research persist-before-browse note (lightweight `research.md` convention; capsule design retired per external prior-art) | framework | governance | P2 | — | quick-win | Shipped | [#251](https://github.com/KbWen/agentic-os/issues/251) | — |
| 77 | D — Reviewable Task→Step hierarchy | framework | governance | P1 | — | architecture-change | Pending | [#252](https://github.com/KbWen/agentic-os/issues/252) | — |
| 78 | B+C — Task capsule + one read-only reviewer (two verdicts) | framework | governance | P1 | — | feature | Pending | [#253](https://github.com/KbWen/agentic-os/issues/253) | #77 |
| 79 | A — Skill effectiveness evaluation harness | framework | skills | P2 | — | feature | Pending | [#254](https://github.com/KbWen/agentic-os/issues/254) | #77, #78 |
| 80 | G1a — First-party SKILL.md compatibility floor (delta over validate.sh) | framework | skills | P1 | — | quick-win | Shipped | [#255](https://github.com/KbWen/agentic-os/issues/255) | — |
| 81 | G1b — Activated-skill provenance inventory | framework | skills | P2 | — | quick-win | Shipped | [#256](https://github.com/KbWen/agentic-os/issues/256) | — |
| 82 | Karpathy-principles source/license provenance wording | docs | governance | P3 | — | tiny-fix | Shipped | [#257](https://github.com/KbWen/agentic-os/issues/257) | — |
| 83 | Skill/workflow content-optimization pass — research + compare vs reference repos (multica-ai/addyosmani/tech-leads-club/VoltAgent/superpowers/anthropic-skills) for substantive guidance/structure improvements to our skills & workflows | research | skills | P2 | — | research | Cancelled | — | after #80/#81/#82 ship |
| 84 | README→CI onboarding — show adopters how to wire CI as a required, non-bypassable check; README sells "CI is the floor that can't be skipped" but Quick Start only covers `deploy_brain.sh` (reviewer-flagged as the real conversion gap; likely a docs/INSTALL.md section + branch-protection recipe) | docs | adoption | P2 | — | quick-win | Shipped | — | after #262 |
| 85 | Cursor first-class support — a real in-Cursor screenshot/GIF + a Cursor platform guide (Cursor sits in the Compatible tier with no guide while Codex/Claude have one; `CLAUDE_PLATFORM_GUIDE.md` also lacks a `_zh-TW` twin) | docs | adoption | P3 | — | quick-win | Cancelled | — | after #262 |
| 86 | README polish — de-dup the 3× "leaked secret / fake tests / skipped phase" repetition (Rules-vs-enforcement · What-you-get · FAQ) and the 2× cross-platform mention; unify the workflow + pipeline GIFs onto one example task (EN + 繁中) | docs | adoption | P3 | — | tiny-fix | Shipped | — | after #262 |
| 87 | INSTALL zh-TW twin — `docs/INSTALL.md` (incl. the #84 CI-floor onboarding section) is EN-only; create a 繁中 twin for locale parity (Codex/Claude platform guides + EN/繁中 READMEs all have zh twins; INSTALL doesn't) | docs | i18n | P3 | — | quick-win | Pending | — | after #84 |
| 88 | Balance Windows pytest shards via a committed `.test_durations` file (count-split clusters the deploy tests onto one shard; measured wall-clock 7:14 — a durations file would rebalance to ~4 min) | dx | ci | P3 | — | quick-win | Pending | — | — |
| 89 | Work Log enforcement audit — validator checks `## Security Findings` section presence on review/ship logs (C1) + Loaded-Sections receipt presence in `## Session Info` (C2); both honor-system today (every MUST = one enforcement) | framework | governance | P2 | — | quick-win | Pending | [#288](https://github.com/KbWen/agentic-os/issues/288) | — |
| 90 | BOM-tolerance for the central `_yaml_loader.py` (read `utf-8-sig`; parity with the #273 capabilities/provenance BOM fix — the authoritative loader still reads trigger-registry/lifecycle-scenarios as plain utf-8) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 91 | Fix `retro.md` Global-Lessons append misplacement — guard exposes only file-end `--mode append`, but `## Global Lessons` is mid-file so the lesson lands under Ship History (same class as the shipped #265 ship.md fix); switch to section-insert | framework | governance | P3 | — | quick-win | Shipped | — | PR #322 (root-caused as retro.md §3 doc defect; tool already section-locates) |
| 92 | `--list-checks` parity — add `-ListChecks` to `validate.ps1` OR correct `CHANGELOG.md:217` which advertises it jointly for both validators (ps1 lacks it); decide enforce-vs-honest-doc at implement | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 93 | `connecting-a-knowledge-base_zh-TW.md` twin — the zh-TW README `## 文件` table links this EN-only guide (the only row pointing at English); locale parity (sibling to #87) | docs | i18n | P3 | — | quick-win | Pending | — | — |
| 94 | Rename drifted lifecycle-token test names — `test_quick_win_current_total_under_29k` asserts 30000 and `..._under_350k` asserts 354000 (name/assertion mismatch) | framework | test-hygiene | P3 | — | tiny-fix | Pending | — | — |
| 95 | Delete orphaned + duplicate `docs/guides/superpowers-playbook.md` (+ its `.agent/workflows/` stub) — content duplicates the phase tables already in reference.md/READMEs; reachable only via the stub | docs | cleanup | P3 | — | tiny-fix | Shipped | — | PR #321 |
| 96 | Link `docs/ai-contributors.md` from a human-facing index (README Docs table or CONTRIBUTING) — human-facing contributor guide currently reachable only from copilot-instructions + a guard test | docs | discoverability | P3 | — | tiny-fix | Pending | — | — |
| 97 | routing_actions staleness escalation — `validate.sh`/`.ps1` WARN on `status: pending` routing actions older than N days (config-driven, parallel to existing lock + spec-index staleness checks) so a cross-domain orphan can't sit unwatched; today routing_actions get field-validation + a ship-time check only for pending actions targeting the *current* primary_domain | framework | governance | P3 | — | quick-win | Shipped | [#306](https://github.com/KbWen/agentic-os/pull/306) | — |
| 98 | SSoT section-targeted append for Spec Index + Ship History — `/ship` writes `current_state.md` via whole-file CAS replace + manual §11.1 additive-merge; the only structural many-concurrent-ship bottleneck for large teams. Convert these append-only sections to guarded `--mode append` (already proven, 50 concurrent → 50 lines). Backlog-only: no verified consumer today (solo repo, zero concurrent ship) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 99 | Partial / minimal adoption on-ramp — deploy is all-or-nothing (~2600 files); no `deploy --safety-only` to take just the credential floor + safety nucleus (already factored standalone per ADR-008). Doc-first ("copy these 3 files") may discharge it; selector only if a real adopter wants floor-without-workflow | dx | adoption | P3 | — | quick-win | Pending | — | — |
| 100 | Subagent fan-out under autopilot: same-owner Work Log lock contention — `subagent_policy: read-only` avoids it; lock short-circuit deferred (ADR-007, no consumer). Report-trigger issue for real autopilot adopters | framework | governance | P3 | — | feature | Pending | [#291](https://github.com/KbWen/agentic-os/issues/291) | — |
| 101 | Surface adopter's pre-existing skills + the `custom-*` on-ramp at bootstrap — present-only opt-in detector (never auto-activate, ADR-007). Report-trigger issue for adopters with existing skill packs | framework | skill | P3 | — | feature | Pending | [#292](https://github.com/KbWen/agentic-os/issues/292) | — |
| 102 | Legacy poisoned-manifest migration (`baseline_mode: upstream`) — conditional follow-on to the v1.8.4 deploy data-loss fix: manifests written by ≤v1.8.3 may carry a preserved *user* hash as the baseline; a `baseline_mode` marker + one conservative sidecar cycle for unmarked manifests would stop a poisoned baseline from overwriting user content on the next upgrade. Backlog-only (needs a real ≤v1.8.3 install hitting it); the v1.8.4 fix stops NEW poisoning | framework | deploy | P3 | — | quick-win | Pending | — | after v1.8.4 |
| 103 | Governance premortem 2026-07-01 follow-ups — the 4 `status: pending` routing_actions in `docs/reviews/2026-07-01-governance-premortem.md`: (a) collision-resistant Work Log keys / explicit-resume confirmation for default/protected branches; (b) explicit reduced-assurance receipt labeling for ship/gate claims on no-file-access platforms; (c) ship-time risk-reconciliation check for quick-win governance/deploy/trust-boundary changes; (d) validator WARN taxonomy split (hygiene vs governance-integrity debt vs reduced-assurance). Keep each routing_action `pending` until absorbed into `docs/architecture/document-governance.md` or rejected. Backlog-only (premortem findings, no observed incident) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 104 | Governance self-audit workflow — there is no dedicated phase/workflow for auditing the governance system ITSELF; `/audit` is scoped to legacy-repo onboarding ("map an existing legacy repository", routing trigger "new module, no ADR"), so governance premortems are run ad-hoc by overloading `/audit` (see the two 2026-07-01 premortem snapshots + the 2026-07-02 two-wave self-audit). A `/govern-audit` (or `/premortem`) workflow would give it a first-class, repeatable home with routing_actions + snapshot naming baked in. From 2026-07-02 self-audit (A7) | framework | governance | P3 | docs/specs/governance-self-audit-workflow.md | feature | Shipped | — | PR #311 |
| 105 | Deploy version-drift signal — downstream deploy records `version` in `.agentcortex-manifest` but nothing ever compares it to the framework version, so a fork running a months-old validator gets no "you are N versions behind" signal. Add an advisory drift check (validator WARN or deploy notice) comparing manifest version to the shipped banner. From 2026-07-02 self-audit (deploy agent) 2026-07-10: also cover the non-transactional deploy half-upgrade state (manifest written last; interrupted deploy leaves mixed-version files with a stale manifest and nothing detects it). | framework | deploy | P3 | — | quick-win | Pending | — | — |
| 106 | Work-log local credential scan — `scan_credentials.py` runs on the git diff/staged set, but `.agentcortex/context/work/*.md` is gitignored, so a secret pasted into a Work Log is never scanned by CI or the (opt-in) pre-commit hook. Add an opt-in local/pre-commit scan pass over `work/` (local-only; CI can't see gitignored logs). Low blast radius (local ephemeral), honesty-motivated. From 2026-07-02 self-audit (seams agent) | framework | security | P3 | — | quick-win | Pending | — | — |
| 107 | Eval coverage partial-match precision — `run_governance_eval.py` `_run_coverage` matches a case's `protects:` to the rule inventory via a bidirectional substring test (`norm_protects in rule OR rule in norm_protects`), which can mis-attribute a case to a longer/shorter anchor and over/under-count coverage. Tighten to exact-or-explicit-prefix. Also normalize the one `### §4.5` heading (literal § → inventory emits `§§4.5`) so the anchor stops being a special case (update the guarding case's `protects:` in the same change). From 2026-07-02 self-audit (eval agent) | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 108 | `verify_agent_evidence.py` `parse_evidence()` accepts incomplete stanzas — an entry with a `command:` but no `result:`/`summary:` is saved as `EvidenceEntry(..., result='', summary='')` instead of being rejected/flagged. Add a completeness guard so partial evidence stanzas are surfaced, not silently accepted. From 2026-07-02 self-audit (python-tools agent) | framework | tooling | P3 | — | tiny-fix | Pending | — | — |
| 109 | Reclassification has no timing guard — completeness/gate checks use the FINAL classification, and the H4 reset is count-based on Drift Log records with no check that the reclassification happened BEFORE implement; a post-implement feature→quick-win downgrade can shed the review/handoff required-set. Related to #103(c) quick-win risk reconciliation. Local-only enforcement (work/ gitignored, CI-invisible). From 2026-07-02 self-audit (gate-bypass agent, A3) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 110 | Hotfix no-test-runner dead-end — `test.md` correctly refuses a PASS test receipt when no test runner exists (even with user sign-off), but `ship.md` requires a test receipt for hotfix → an emergency fix on a runnerless host has NO legal path to ship. Needs a policy decision: reduced-assurance hotfix ship lane (explicit label + user sign-off) vs escalate-to-feature mandate. From 2026-07-02 wave-3 walkthrough (C3) | framework | governance | P2 | — | quick-win | Pending | — | — |
| 111 | Test-teeth hardening batch — from the 2026-07-02 test-quality audit: (a) AC-8 credential-scan tests: `test_ac8_rc3_not_exit_zero` block-end detection coupled to exact YAML indentation, `test_ac8_base_sha_missing_still_exits_zero` over-broad substring; (b) `test_codex_and_claude_deltas_differ` asserts nothing its name claims; (c) framework-live-state fixtures (#170/#172 no-warn tests) should pin to a deployed temp fixture; (d) verify windows-latest runner actually has Git-Bash (else all sh↔ps1 behavioral parity tests silently skip everywhere). Batch of assertion-precision fixes, no engine change 2026-07-10 additions: (e) validate.ps1 illegal-gate-progression has NO FAIL-path behavioral test (only the legal-case ps1 sibling exists — test_validator_false_positives.py:1019/:1045); (f) PR #314's F1/F2 fixes are guarded by source-substring asserts only, never executed behaviorally (:841-867) while adjacent D4 checks show the behavioral pattern (:886-910); (g) tests/guard/test_ssot_heartbeat_contract.py:7-8 docstring falsely claims .agentcortex/tests/test_ssot_completeness.py is not CI-run (collect-only proves it is). | framework | test-hygiene | P3 | — | quick-win | Pending | — | — |
| 112 | CI docs-only gate hole — README/INSTALL/spec content-pin tests (`test_readme_fork_guidance_parity`, `test_text_only_install_does_not_copy_runtime_context`, `test_ac4_ac5_readme_documents_pre_commit_hook_setup`, `TestClaimsVsReality`) live in pytest jobs gated `heavy == 'true'`, and README/docs paths classify inert → the pins never run on the docs-only PRs most likely to break them. Fix direction: a fast ungated "docs-pins" job (pure file-read tests, seconds) that runs when heavy=false. Also: audit-chain git append-only witness has NO behavioral test (only source-substring presence) — add slow behavioral tests (truncated/edited INDEX in a temp git repo). From 2026-07-02 test-quality audit (both P1s) | framework | ci | P2 | — | quick-win | Shipped | — | PR #310 |
| 113 | `--no-python` validator undersells reduced assurance — the bash gate-progression fallback checks only that plan+implement receipts are PRESENT, not phase ordering/completeness, so in `--no-python` mode a feature Work Log that skips review/test/handoff (but keeps plan+implement) prints top-line "integrity check passed". The only tell is the SKIP count jump (13 vs 4). Fix direction: the fallback can grep required-receipt PRESENCE per classification (no Python needed for presence-counting, only for ordering-legality), OR the top-line verdict should say "reduced assurance" when python is off. Connects #103b reduced-assurance labeling. From 2026-07-02 stability-sim red-team (Attack 9) 2026-07-10: also a cross-validator verdict divergence — on a no-Python host validate.sh degrades gate-progression to the narrow ship-missing-prereq fallback (WARN) while validate.ps1 runs the full native parser (FAIL) → same log, different verdicts by platform. | framework | governance | P2 | — | quick-win | Pending | — | — |
| 114 | tiny-fix self-labeling has no validator visibility — a large semantic change mislabeled `tiny-fix` ships with one receipt and a clean pass because tiny-fix has NO Work Log (validator never sees it) and `required=set()`. No file-count/diff-size check exists. Honor-system limit inherent to the no-worklog fast path; a real guard would need a pre-commit/CI diff-size heuristic outside the work-log validator. Related to #103c quick-win pressure-valve. From 2026-07-02 stability-sim red-team (Attack 3) | framework | governance | P3 | — | quick-win | Pending | — | — |
| 116 | check_command_sync EXPECTED_COMMANDS coverage gap — the checker tracks 27 of 30 `.claude/commands/` stubs (`app-init`, `execute-plan`, `write-plan` untracked), so a deployed adopter counting files vs the printed "N commands verified" sees a confusing mismatch; decide track-all vs document-exclusions at implement. Pre-existing, surfaced by the PR #316 fresh-adopter deploy sim | framework | tooling | P3 | — | quick-win | Shipped | — | PR #321 |
| 117 | Global Lessons cap-archival vs hash-chain contradiction — retro.md §3 says archive oldest LOW entries at cap, but check_lesson_chain.py breaks on ANY removal (head must be GENESIS-anchored; each prev hashes its immediate predecessor), so the documented procedure is unexecutable; append_lesson.py refuses at cap (hit for the first time 2026-07-04 at 20/20). Decide: chain-aware archival (move entry + re-anchor successor + audit record in archive) vs cap bump vs HIGH-pin-only. Blocks new Global Lessons until resolved | framework | governance | P2 | — | quick-win | Shipped | — | PR #322 (chain-aware archival; residual: append the pending 2026-07-04 lesson via /retro) |
| 118 | Claude adapter bootstrap hard-stop sync — `.claude/commands/bootstrap.md` unconditionally "STOP and ask" even when the user explicitly requested a downstream phase in the same message, contradicting AGENTS.md §3/§6 same-turn continuation; costs every Claude Code adopter one extra turn per task. Fix stub wording (+ audit sibling stubs for the same pattern; verify Codex/Antigravity adapter parity). From 2026-07-06 full audit (primary-verified) | framework | governance | P2 | — | quick-win | Shipped | — | PR #321 |
| 119 | Design Gate no-DSoT escape hatch — plan.md §Design Gate (+ engineering_guardrails.md §4.4) hard-fails `missing: [design_link]` on any user-visible UI plan; a tool-less adopter is hard-blocked. **Resolved v1.8.9 (R1)**: §4.4 already accepts a "URL **or file path**" artifact — the DSoT definition + both /plan stop messages now name a committed Markdown/ASCII wireframe file (`docs/design/<screen>.md`) as valid; gate stays HARD (no artifact still hard-stops). **The capability-seam `design_tool` escape (this row's original suggestion) was DESIGNED then roundtable-REJECTED (5 agents + 第十人 + 事前驗屍): it reincarnates the ADR-001-D2-rejected flag AND breaks `validate_downstream_capabilities.py`'s gate-relaxation-UNREPRESENTABLE allowlist — do NOT retry.** From 2026-07-06 full audit | framework | governance | P2 | — | quick-win | Shipped | — | PR #324 (v1.8.9) |
| 120 | Deploy-end enforcement onboarding (conversion wedge) — a default install ends with ZERO active machine enforcement: pre-commit hook is opt-in, deploy adds no CI workflow, and work logs are gitignored so the README "validate.sh reads the work trail" row can never fire in downstream CI. Add: hook-install prompt (or `--with-hooks` flag) at deploy end + printed CI/branch-protection recipe + a post-deploy self-check command the adopter can run immediately. Highest-leverage conversion fix from the 2026-07-06 audit; pairs with the standalone "secret gate in one command" story (#99) | dx | adoption | P1 | — | feature | Pending | — | — |
| 121 | README enforcement-matrix + 3-gems reframe — front-load the three immediately-usable wins (standalone credential gate; work-log + /handoff cross-session memory; 14 expert skill checklists) and add an honest enforced-vs-wire-up-vs-advisory matrix; current hero copy reads as default-on enforcement. CAUTION: README content is pinned in 2 layers (validate.sh/.ps1 encoding-canary phrases + tests/ci docs-pin pytest) — grep tests/ci + both validators BEFORE restructuring 2026-07-10 scope adds: (a) FAQ entry explaining the ⚡ ACX sentinel + gate blocks (currently zero user-facing explanation — README's only ACX hit is the .acx-incoming filename); (b) README_zh-TW.md:78 parity for the EN :82 caveat naming the 3 required checks + security-scans-not-required-by-default. | docs | adoption | P1 | — | quick-win | Pending | — | after #120 |
| 122 | Honor-system MUST deletion pass — per the [enforcement] Global Lesson ("every MUST = 1 hook OR validator OR test OR external observer; rules without enforcement should be DELETED"), sweep AGENTS.md + engineering_guardrails.md for MUSTs with no machine backing and delete, downgrade to guidance, or add the enforcement. Each deletion buys token headroom and restores credibility of the enforced set. One decision per PR where feasible. **Verified instance (2026-07-08 #119 roundtable): the §4.4 Design Gate is honor-system — ZERO validator checks `design_link` (confirmed). Enforce-vs-delete decision OPEN: (a) a T1 plan-lint FAILing a prod-UI plan that cites no `Design:` path (theatre→real enforcement, fits "machine-enforced not self-report"), OR (b) delete the gate (第十人's argument). CAUTION: enforcing ADDS adopter friction — weigh against the conversion thesis (#120/#121).** 2026-07-10 instances: lock acquisition is never verified anywhere (an agent that never calls recover_worklog_lock.py ensure is undetectable — TestGovernanceWiring is string-presence only); verify_agent_evidence.py stays deliberately unwired (CHANGELOG: dropped as vacuous) — treat as the documented exemplar of refuse-vacuous-wiring. | framework | governance | P2 | — | quick-win | Pending | — | — |
| 123 | Worked-example walkthrough — one real feature traced end-to-end (bootstrap→ship) with actual artifacts (spec, gate blocks, receipts, PR link) so a day-1 adopter sees the payoff without reading 49KB of bootstrap.md; link from README + INSTALL first-task section | docs | adoption | P3 | — | quick-win | Pending | — | after #121 |
| 124 | Stale P1 clusters revalidation — #1/#3/#7/#11/#13 (lifecycle engine, 2026-04-12) and #69/#77/#78/#79 (RPI-QRSPI + task capsule + skill harness) have ~3 months zero progress. 30-min evidence checkpoint per cluster: does the original pain still exist after the interim fixes (backlog archive split, token ratchet, #97 staleness WARN, worklog archival)? Resolve EVERY row to do-now / refine-to-precise / close-with-reopen-trigger — no silent carry 2026-07-10: month-3 lifecycle audit supplies the revalidation evidence — unbounded-accumulator pain confirmed live (Ship History 67, INDEX 99 lines, archive 1.3MB, backlog 60 rows, sidecar-per-release); cluster is NOT stale, re-prioritize at next pick. | research | governance | P2 | — | research | Pending | — | — |
| 125 | bootstrap §0a first-task interruption softening — a fresh deploy has an empty `docs/adr/`, so the adopter's very FIRST task is interrupted by the /app-init 5-question interview (Exit 2). Make it non-blocking: one-line recommendation + proceed, or fire only when the first architecture-change classification appears | framework | governance | P3 | — | quick-win | Pending | — | — |
| 126 | Claude stub guardrails-slim — 10 `.claude/commands` stubs listed `.agent/rules/engineering_guardrails.md` as an unconditional Required-read, contradicting CLAUDE.md step 4 / guardrails Quick-Skip Mode / bootstrap.md TOKEN LEAK BLOCK; minimal fix removes just that line (ask-local's §8.2 citation is functional, kept; the other stubs' Required-reads blocks stay — cosmetic only, closed) | framework | governance | P2 | — | quick-win | Shipped | — | PR #327 |
| 127 | SSoT caps enforcement + ship-history archival — current_state.md Ship History hit 67 entries vs ship.md:208 cap 10 (~26k tokens ≈81% of SSoT re-read every bootstrap); spec_index_max_entries:30 config key had NO validator consumer (dead cap). One-time archival to archive/ship-history-2026.md + new check_ssot_caps.py (WARN-tier, both validators, ADR-006 pattern) + config key ship_history_max_entries:10 + NOT-READY re-review remediation hint in both validators' gate parser (message-only; the illegal:plan->review FAIL was correct but cryptic) | framework | governance | P2 | — | quick-win | Shipped | — | PR #328 |
| 128 | Gemini adapter validator parity — validate.sh checks claude_required_files + codex rules but ZERO Gemini surfaces; GEMINI.md (15 lines, @imports AGENTS.md, deploys downstream as scaffold) can rot/vanish undetected. Fix: presence + content-pin (pin the stable `@AGENTS.md` import line) in both validators. EXPLICITLY CLOSED with reopen-trigger: building .gemini/commands dispatch tree or GEMINI_PLATFORM_GUIDE — speculative over-build, zero Gemini-adopter evidence (matches #85 Cursor precedent); reopen on first real Gemini adopter signal or a governance change needing per-command Gemini files | framework | governance | P3 | — | quick-win | Pending | — | — |
| 129 | AGENTS.md scaffold-tier ossification signal — AGENTS.md is preservation-tiered (deploy.sh scaffold): an adopter who edits it directly gets every upstream governance/safety fix silently diverted to AGENTS.md.acx-incoming forever, with no staleness signal; carries Core Directives + safety floor. Design needed: advisory drift signal (e.g. deploy-end notice or validator WARN when .acx-incoming sidecars linger), NOT a tiering change (ADR-005 settled). From 2026-07-10 month-3 lifecycle audit | framework | deploy | P2 | — | quick-win | Pending | — | — |
| 130 | knowledge_sources path-rot advisory — validate_downstream_capabilities.py checks path values are non-empty strings but never existence; a moved/deleted KB path silently degrades to UNREADABLE at bootstrap with no user-facing WARN. Add existence WARN (gate-safe additive; respect `${ACX_KB_PATH}` env resolution + BYO-no-manifest cases). From 2026-07-10 lifecycle audit | framework | governance | P3 | — | quick-win | Pending | — | — |
| 131 | Global Lessons all-HIGH deadlock escape — append_lesson.py hard-blocks at cap 20 AND --archive refuses HIGH entries; at 20/20 all-HIGH the learning loop deadlocks with no self-service escape (framework repo already at 10 HIGH of ~20). Policy decision needed: HIGH-demotion path with user sign-off vs cap bump vs HIGH-consolidation lesson-merge. From 2026-07-10 lifecycle audit | framework | governance | P2 | — | quick-win | Pending | — | — |
| 132 | Work-log cancel/abandon path — no /cancel workflow exists; abandoned tasks' frozen work logs accumulate until a manual archival chore (INDEX shows ~20 such backfill/stale-archival records). Design: a lightweight cancel disposition (archive with status: cancelled + INDEX entry, no ship receipts required). From 2026-07-10 lifecycle audit | framework | governance | P3 | — | feature | Pending | — | — |
| 133 | Draft-spec staleness WARN — specs sit in status:draft indefinitely with zero nag (routing_actions get a 14-day staleness WARN; drafts get none). Mirror the config-driven pattern (draft_spec_warn_days). From 2026-07-10 lifecycle audit | framework | governance | P3 | — | quick-win | Pending | — | — |
| 134 | Validator honesty pair — (a) templates/current_state.md ships `- **Active Backlog**: (none yet)` which matches NEITHER validate.sh:2197's bare-`none` FAIL branch nor the backtick path extractor → the intended FAIL is unreachable on template-default SSoTs (needs a design look: template value vs regex, avoid failing fresh deploys); (b) worklog template `## Evidence` ships a `>` guidance blockquote that survives body extraction so a placeholder-only Evidence section reads non-empty and passes the shipped-empty-Evidence check (validate.sh:1572-1574 / ps1:1465-1468). From 2026-07-10 doc-consistency audit | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 135 | Quick-win-lite worklog template — the 17-section/13-field template is identical for a 5-line quick-win and an architecture change; a classification-aware lite variant would cut agent-borne ceremony ~50% but touches the validator's required-section sets + tests. Parked pending conversion-first thesis (#120/#121 outrank). From 2026-07-10 ceremony audit | framework | governance | P3 | — | quick-win | Pending | — | — |
| 136 | Gate-progression FAIL-count parity — bash's completeness check breaks at the first failure (1 FAIL contribution per malformed log) while ps1 falls through into the progression-order loop and double-counts (bash reports 2, ps1 reports 4 on the same 2 malformed fixtures). Verdicts agree, counts drift — cosmetic-but-confusing cross-platform divergence. Found during WP3 receipt-integrity verification (see PR #341 Work Log Known Risk) | framework | tooling | P3 | — | quick-win | Pending | — | — |
| 137 | Promote check_routing_actions.py downstream — the structural routing_actions check is source/CI-only; downstream deploys keep the weaker native backstop by deliberate WP1 decision. Promotion = deploy.sh/ps1 runtime_tools + manifest golden + the 2 deployed-fixture slow tests updated as a set (the #334 class of gap, this time tracked not accidental). From 2026-07-11 audit-wave WP1 (PR #340) | framework | tooling | P3 | — | quick-win | Pending | — | #113 |
| 138 | Decision-capture hardening — decide.md §5 "promote to ADR during /ship" is an untriggered orphan: ship.md never reads Work Log ## Decisions (its §7 reads only the spec's Domain Decisions, feature/arch only), zero validator/test/eval enforcement, worklog template lacks the section, quick-win/hotfix have no spec channel and the L2 nudge is user-gated (headless no-op). Evidence: 82/113 archived INDEX.jsonl entries carry decisions with empty specs[]; 3 of 4 live Ship History decision clusters unbacked. Fix shape: enforce-or-delete §5 + ship-time decision disposition (promote → ADR/L2 or mark local) + WARN-tier validator check behind the ADR-006 seam — DELETION-FUNDED (token headroom ≈0) + sh/ps1 parity. From 2026-07-16 decision-capture audit. Shipped 2026-07-16 (branch feat/decision-capture-hardening: ship.md 2b + tripwire, decide.md §5 rewrite, template §Decisions, check_decision_disposition.py Signal A+A2, bootstrap none-guard; spec decision-capture-hardening.md) | framework | governance | P2 | docs/specs/decision-capture-hardening.md | feature | Shipped | — | — |
| 141 | Archived-worklog Phase-Summary audit false-positives on non-worklog archive artifacts — `archive/global-lessons-archive.md` (first created by /retro chain-aware archival 2026-07-19) tripped the scan; `test_171_ship_history_no_phase_summary_warn` enforces zero such WARNs framework-wide, so the "defer" disposition was invalid and PR #353 merged red (the #270-class incident, operator-caused: unconditional `;` merge chain + subset-only local tests). Fixed same-day: exclusion filter extended to `global-lessons-archive*` in both validators (sh -iname + ps1 -notlike, parity). Residual: promote the filter to a documented non-worklog allowlist when a third artifact class appears. | framework | tooling | P3 | — | quick-win | Shipped | — | #140 |
| 140 | Validator parity: archived-worklog `## Phase Summary` heading match is PREFIX in validate.sh (awk `/^## Phase Summary/`) but EXACT-then-newline in validate.ps1 (regex `^## Phase Summary\s*\r?\n`, ps1:1996) — a suffixed heading (e.g. `## Phase Summary (full entries)`) PASSes sh but WARNs ps1, breaking `test_validator_count_parity_on_framework` (exposed 2026-07-19 by the /handoff §6 compaction-overflow file on PR #352; hotfixed by exact-heading rename in the overflow artifact). Fix = align both parsers to the same match rule (prefer prefix-or-exact, sh+ps1 in one change) + a parity fixture with a suffixed heading. | framework | tooling | P3 | — | quick-win | Pending | — | #136 |
| 139 | Durable homes for orphaned live-window decisions — (a) design_tool/ADR-011 unanimous rejection: amend ADR-001 (per its own 2026-06-11 amendment precedent) + a document-governance.log.md L2 entry; today it lives only in Ship History pos-7/10 (ejected in ~4 ships), Shipped-filtered #119 prose, and the one review snapshot lacking a routing_actions block; (b) same pass evaluates the ship-history-cap-10 rationale + NOT-READY FAIL-preservation decision and the point-in-time archival precedent. Soft deadline: before ~4 more ships. From 2026-07-16 decision-capture audit. Shipped 2026-07-16 (branch chore/orphan-decision-homes; instance-batch only — #138 systemic fix stays open) | framework | governance | P2 | — | quick-win | Shipped | — | #138 |

> Rows whose GH issue is CLOSED-premature (#7, #13, #18, #21, #33) are kept deliberately as
> future directions — reopen the issue when a concrete signal appears (2026-06-02 curation).
> Rows #76–82 (added 2026-06-19) came from the external skill/workflow-practices research (Node-15 synthesis). #76 was reduced to a lightweight `research.md` note-taking convention and the heavier capsule design (ADR-009 + draft spec) retired — external prior art (Anthropic context-engineering + multi-agent research system) treats research-state persistence as lightweight "structured note-taking", not infrastructure, and agentic-os already externalizes to Work Logs / private notes. **Reopen-triggers (record-only, no issue opened):** G2 byte/installer-verification → when an external skill-installation capability is separately approved; E context-free author testing → when #79 is built; F isolation-aware worktree portability → when a non-git-worktree platform target appears (must not duplicate existing worktree rules).
> 2026-06-19 (Option A): #80+#81 merged on branch `feat/skill-provenance` as ONE quick-win ("SKILL.md compatibility + provenance floor"); #81 right-sized feature→quick-win (static manifest + completeness check); its heavy half (digests/byte-verify/exception-schema) stays under the G2 reopen-trigger above. #82 rides along. #83 (skill/workflow content-optimization research vs reference repos) added, queued AFTER this branch ships.
> Rows #84–86 (added 2026-06-20) are the un-done follow-ups from the README revisual (**PR #262**, merged `318d8f2` — a 3-visual hook→proof→system overhaul, EN + 繁中, with `concept-hero`/`workflow-demo`/`pipeline-demo` in both languages). Full context + what shipped is in the `Ship-docs-readme-revisual-2026-06-20` entry of `current_state.md`. **#84 (CI-onboarding) is the highest-value** — a final-review persona called it the real conversion leak (the README leans on "CI is the unbypassable floor" but never shows an adopter how to wire it). #85/#86 are parity + polish.
> Completed entries (#2, 4–6, 8–10, 12, 14–16, 19–20, 22–32, 34–44, 51, 66–68, 71, 73–75) are in [`_product-backlog-archive.md`](./_product-backlog-archive.md).

> 2026-06-20 batch close-out: #84 shipped (PR #263). A cross-platform entry-point diagnosis + the README follow-ups spawned: **Copilot deploy parity** (PR #264 — `.github/copilot-instructions.md` now ships downstream), **ship.md Ship-History ordering fix** (PR #265), **#86 README de-dup prose** (PR #266), and the **using-git-worktrees Step-0 DO-NOW** from #83 (PR #267). **#86 → Shipped** (prose de-dup done; the GIF-unification half is a reopen-trigger — needs binary-asset re-render + visual verification we can't reliably do). **#83 → Cancelled** (skill content verified SOUND vs superpowers v6 by independent experts; broad "needs content optimization" premise disconfirmed; only the worktrees DO-NOW was actionable → PR #267; reopen only if a concise skill is shown to cause a real failure). **#85 stays Pending, blocked on owner Cursor access** — the Cursor compatibility *claim* is VERIFIED (Cursor natively reads AGENTS.md; PR #264 diagnosis), so the residual deliverable is a real in-Cursor screenshot/GIF (owner-only) + an optional Cursor platform guide + the `CLAUDE_PLATFORM_GUIDE.md` `_zh-TW` twin.

> 2026-06-20 (zh-twin ship, branch `docs/claude-guide-zh-tw`): **#85 → Cancelled.** The `CLAUDE_PLATFORM_GUIDE_zh-TW.md` twin (the AI-doable residual) shipped, now deploys downstream (deploy.sh glob + managed[]), and is linked from the 繁中 README. Verified the **Cursor (AGENTS.md native read; README:159/195) + Copilot (`.github/copilot-instructions.md`, deployed #264) entry points already ship** — so an earlier "cancel Cursor support" framing was WRONG; what's cancelled is only the **owner-only in-Cursor screenshot/GIF** (owner has no Cursor — marketing polish, not a functional gap). **Reopen-trigger:** owner gains Cursor access + wants the visual. Cursor platform guide **declined** (thin; README/reference already document the Cursor entry). Spawned **#87** (INSTALL zh-TW twin — the #84 EN-only `docs/INSTALL.md` residual).

> 2026-06-28: #97 surfaced by a governance-only comparison sweep against a reference implementation; verified absent here (routing_actions has field-validation + a ship-time current-domain check, but no age escalation — a pending action for a non-current domain can persist unwatched). Backlog-only per issue-exposure policy (small hardening of an existing gate, not feature-like; no observed incident → P3).

> 2026-06-28 (downstream-adaptability research): #98–101 from a read-only 3-axis adaptability diagnosis (skill coexistence · multi-person scaling · complex adoption scenarios). #98/#99 backlog-only — no verified consumer today (solo repo / no floor-only request). #100/#101 are report-trigger **issues** (#291/#292), feature-like, opened to gather real adopter signal per issue-exposure policy *before* building. Most adaptability axes were already-handled or out-of-scope-by-design (ADR-004/005/007/008); the three small do-now honesty/hardening fixes (skill-ecosystem status note, INSTALL monorepo note, bootstrap owner-default) shipped directly, not via backlog.

> 2026-07-06 (full-project audit routing): #118–#125 route the 2026-07-06 three-agent audit + primary spot-verification (overall grade C; root-cause thesis: discovered-not-converting because a default install has zero active machine enforcement and day-1 is interruption-dense — **fix conversion, not features**). Dispatch this session: #95+#116+#118 → branch `fix/audit-quickwins-batch`; #117+#91 → branch `fix/lesson-chain-archival` (both touch append_lesson.py, batched to avoid conflict). Suggested pick order for follow-up sessions: #120 → #121 → #119 → #113 → #122 → #124 → #125 → #123. **Watch-items (record-only):** (a) token ceiling headroom ≈0 — counted-doc additions stay DELETION-FUNDED; refuse scope past ~345k without a paired deletion; (b) two 2026-07-06 subagent claims are UNVERIFIED — re-check before acting: ".agents/ vs .agent/ skills trees are identical full copies" (AGENTS.md documents stub-vs-canonical by design) and "skill-research-integration.md is a shipped spec" (absent from the SSoT Spec Index); (c) sentinel enforcement nuance for future audits: the work-log ⚡ ACX marker HAS a WARN-tier validator check (validate.sh ~L1509/L1702) — only the per-response sentinel is self-attested; (d) #120/#121 are the adoption bet — new features stay frozen until conversion signal (stars/issues/return visits) moves.

> 2026-07-09 (post-v1.8.9 reconcile): **#119 → Shipped** via v1.8.9 (PR #324) as **R1 — a framing clarification** (§4.4 already accepted a file-path artifact; the gate stays HARD), NOT the capability-seam escape this row originally proposed. A 5-agent roundtable + 第十人 + 事前驗屍 UNANIMOUSLY rejected the `design_tool` capability-seam path (verified: reincarnates ADR-001 D2's rejected flag; `validate_downstream_capabilities.py`'s allowlist makes gate-relaxation unrepresentable → an unknown key rejects the whole file). The deeper finding — the design gate is honor-system theatre — folds into **#122** (enforce-vs-delete), NOT a new row. v1.8.9 also packaged already-merged #319/#321/#322 (adapter same-turn #118, Global-Lessons chain-archival #117, ask-local sync). **Full roundtable record (cross-platform)**: `docs/reviews/2026-07-08-design-gate-roundtable.md`.

> 2026-07-10 (complaint-driven audit routing): #126-#135 route the 2026-07-10 four-angle audit (day-1 friction / ceremony-cost / enforcement-gap / active-harm) + 3 second-wave finders (test-teeth, doc-consistency, month-3 lifecycle) + 2-expert panel adjudication. Dispatched this session: #126 (branch fix/claude-stub-guardrails-slim), #127 (chore/ssot-caps-enforcement), doc-consistency batch (fix/doc-consistency-batch: worklog-template Test Gate Results gap, zh-TW gate-critical mirror drift ×2, state_machine escalation-list subset, AGENTS.md quick-win transition wording, routing.md skill/command registry gaps). Closed-with-reason (no rows): shared-contracts per-phase re-read (deliberate Read-Once exemption, AGENTS.md:27); deploy add-only accumulation (ADR-005 never-delete by design; reopen on adopter gc request); backlog row growth (manual archival chore precedent); docs-PR heavy-test skip (dev-flow-hardening AC-13 deliberate; #112 shipped the docs-pins job); the 12 non-guardrails Required-reads stub blocks (cosmetic). Conversion thesis unchanged: #120 → #121 remain the top picks; none of #126-#135 outrank them.

> 2026-07-11 (codex audit-wave remediation): codex two-pass + adversarial governance audits (PRs #337/#338 — 3 review snapshots, 10 findings, deduped against #105/#108/#109/#113/#114/#116/#120/#122/#134) → **all 10 primary-verified real, 0 false alarms** (F10 broader than reported: punctuation chars break current-branch detection on BOTH platforms, not just case on bash) → remediated same-day in 3 delegated PRs, all merged CI-green: **#339** (executor safety — §8.2 canon: pre-flight baseline capture, abnormal-exit stop-and-reconcile, baseline-dirty never whole-file-reverted, Requested/Actual Executor provenance; enforced by 7 docs_pin tests), **#340** (validator fail-open — directive-anchored command-sync, manifest-agnostic adapter validation, structural routing_actions parse via check_routing_actions.py behind the ADR-006 seam; 17 new tests), **#341** (receipt integrity — bootstrap 5-step canonical worklog-key normalization in both validators + Timestamp/Classification/SHA value validation on the existing WARN-tier checks; 19 new fixtures). All 10 routing_actions rows flipped merged; L2 document-governance entry appended. New rows: #136 (ps1 FAIL double-count parity, found by WP3 beyond the audit), #137 (routing-tool downstream promotion, deliberate WP1 non-goal). Closed-with-reason (no rows): Timestamp monotonic ordering (order-of-appearance authoritative; clock-skew false-positives); reclassification epoch parsing (WARN-tier header binding suffices); GPT-1.0 residual wording + vestigial `.agents/workflows/` duplicates (both fixed/deleted in the wrap-up PR — zero references, verified both directions). Conversion thesis unchanged: #120 → #121 remain the top picks.

> 2026-07-16 (decision-capture audit routing): #138–#139 route the 2026-07-16 govern-audit (user-hypothesis verification: "product decisions land SSoT-only without ADR/spec → drift"; 3 scan + 2 simulation agents + primary verification — confirmed **real-but-bounded**: every ADR/spec-backed decision found durable; the leak is quick-win/hotfix decisions + rejected designs + cap-10 rotation). Closed-with-reason (no rows): bootstrap L1-only/L2-write-only reads (token economy by design), Ship History cap-10 rotation itself (#127 by design — the defect is upstream decision homelessness), Symphony-model 2026-05-06 loss (pre-dev-flow-hardening; reopen trigger: grep archive/feat-epic-spec-hierarchy-governance-20260506.md before any backlog-schema redesign). Full record + routing_actions: `docs/reviews/2026-07-16-govern-audit-decision-capture.md`. Conversion thesis unchanged: #120 → #121 remain the top picks; #139 carries a rotation deadline (~4 ships).

> 2026-07-16 (decision-capture wave close-out): **#139 → Shipped** (PR #348, instance-batch) and **#138 → Shipped** (feat/decision-capture-hardening: ship.md 2b + SIM-W tripwire, decide.md §5 truth-rewrite, template §Decisions, check_decision_disposition.py Signal A + A2 with non-clearing remediation, bootstrap:143 none-guard; both audit routing_actions → merged). Verified by 第十人+事前驗屍 (12 dispositions) + a 4-sim behavioral wave (write-path, read-path drift replay — original scenario now hard-stopped rotation-proof, violation loop, downstream deploy ×4). **Watch-item (record-only, no row)**: config.yaml is deploy-tier core (force-updated on upgrade) so fork-tunable keys (production_paths, decision_disposition_since) do not survive framework upgrades — pre-existing tension, no observed incident; reopen if a fork reports lost config customization or an upgrade WARN-flood.

## Status Key

- Pending: not yet started
- In Progress: spec generated, bootstrap running
- Shipped: feature shipped (see Ship History in current_state.md)
- Deferred: explicitly deferred
